apiVersion: policy/v1beta1
kind: PodDisruptionBudget
metadata:
  name: es-client
  namespace: zerbytes-live-proj-logging
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: elasticsearch
      role: client
