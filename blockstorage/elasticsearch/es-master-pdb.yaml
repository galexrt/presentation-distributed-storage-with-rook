apiVersion: policy/v1beta1
kind: PodDisruptionBudget
metadata:
  name: es-master
  namespace: zerbytes-live-proj-logging
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: elasticsearch
      role: master
